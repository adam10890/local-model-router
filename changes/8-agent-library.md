# Added

- Added a built-in router-backed agent library with catalog and run endpoints.
- Added per-agent routing intent, local-only sovereignty, input limits, and
  timeout handling for Pydantic AI runs.

# Changed

- Windows setup/start/stop scripts now prepare and manage the agent runner.
