# Beta gates baseline

- Strengthened `imperium doctor` from import checks to required callable
  capability checks while preserving existing JSON dependency codes.
- Removed obsolete dashboard mode controls and debug telemetry, kept Advanced
  navigation available, and clarified pinned tool-path labels.
- Aligned the Windows bundle gate with Python 3.12 and package/tag versions.
