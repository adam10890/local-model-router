# Added

- Harness version matrix in `docs/HARNESSES.md` with current-stable checks for
  Hermes v0.20.0 and Pi v0.81.1 alongside verified Agent Zero 2.7.

# Changed

- Extended `scripts/smoke_harnesses.py` with streaming smoke and optional
  `--tools`, and `scripts/smoke_provider.ps1` with `/v1/models` + stream chat.
- Pointed INTEGRATIONS Hermes/Pi sections at the checked release tags and the
  beta evidence log for installed-version recording.
