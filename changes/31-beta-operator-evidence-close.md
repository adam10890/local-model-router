## Changed

- Closed remaining operator NVIDIA beta evidence: live provider stream,
  Hermes harness stream+tools, doctor/update-check, fleet slot restart, and
  manual EN/HE dashboard walk.
- Hardened `scripts/smoke_harnesses.py` with `--harness`, higher timeouts, and
  bare `/v1` URLs for `default` connections.
