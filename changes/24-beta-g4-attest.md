# Security

- Expanded `/config/preview` redaction to cover `authorization`, `credential*`,
  and `passwd` field names in addition to existing API key / token patterns.

# Changed

- Required release-affecting merges to keep `docs/1.0-beta-evidence.md` G4 /
  gate 6 attestation current (`RELEASE.md` check 9).
