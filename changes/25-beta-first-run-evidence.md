# Added

- Operator first-run and recovery checklists in `docs/1.0-beta-evidence.md`
  with concrete cleanroom, CLI, and installer commands for G1/G5 gates.

# Changed

- Expanded packaging hermetic assertions so cleanroom coverage includes
  `/health`, `/ui/status`, and offline `/v1/models`, and so lifecycle
  recovery commands stay documented for operators.
